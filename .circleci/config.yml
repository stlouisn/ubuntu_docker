version: 2.1

jobs:
  build-multiarch-container:
    resource_class: small
    environment:
      C_DESCRIPTION: "ubuntu base image"           # <-- change
      C_MAINTAINER: "stlouisn"                     # <-- change
      C_NAME: "ubuntu"                             # <-- change
      C_TAG: "latest"                              # <-- change
      BUILD_PLATFORMS: "linux/arm64,linux/arm/v7"  # <-- change
    docker:
      - image: cimg/base:stable
        auth:
          username: $C_MAINTAINER
          password: $DOCKER_PASSWORD
    steps:
      - checkout
      # - setup_remote_docker:
      #     version: 20.10.11
      - run:
          name: Labelling dockerfile
          command: |
            export C_VERSION="$(bash .circleci/get_version.sh)"
            bash .circleci/add_labels.sh
      - run:
          name: Preparing dockerfile
          command: |
            sed -i -e '/^$/d' -e 's/^[ \t]*//' -e '/^#/d' dockerfile
            cat dockerfile
      # - run:
      #     name: Configure Docker Buildx
      #     command: |
      #       docker buildx version
      #       echo
      #       sudo apt-get update -qq && sudo apt-get install -y binfmt-support qemu-user-static
      #       echo
      #       docker pull multiarch/qemu-user-static
      #       echo
      #       docker run --rm --privileged multiarch/qemu-user-static --reset -p yes
      #       echo
      #       docker context create buildcontext
      #       echo
      #       docker buildx create buildcontext --use --platform $BUILD_PLATFORMS
      # - run:
      #     name: Configure Docker Hub
      #     command: |
      #       [ -z "$DOCKER_LOGIN" ] && exit 0
      #       [ -z "$DOCKER_PASSWORD" ] && exit 0
      #       echo "$DOCKER_PASSWORD" | docker login --username=$DOCKER_LOGIN --password-stdin
      # - run:
      #     name: Build and push container
      #     no_output_timeout: 20m
      #     command: |
      #       docker buildx inspect --bootstrap
      #       echo
      #       docker buildx build --tag $C_MAINTAINER/$C_NAME:$C_TAG --platform $BUILD_PLATFORMS --progress plain --push .

workflows:
  version: 2
  build-container:
    jobs:
      - build-multiarch-container:
          name: build-docker-ubuntu
